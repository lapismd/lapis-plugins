# Source Editor

Source Editor provides syntax-aware editing for text, JSON, and YAML files.
Supported files open through ordinary workspace routing rather than a separate
utility window.

Edits remain synchronized with the Lapis vault lifecycle, so other views and
services observe the same saved file state.
