# Markdown Lint

Markdown Lint checks open Markdown documents and reports rule violations through
the editor and shared Problems surfaces. Supported diagnostics include code
actions, allowing common issues to be corrected without leaving the document.

Choose disabled rules and include or exclude paths in vault settings to match
the conventions used by your notes.
